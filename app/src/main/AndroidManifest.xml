<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ydn.church">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_contemplation"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScreenTransformActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".MalsseumActivity"/>
        <activity android:name=".MalsseumContentActivity"/>
        <activity android:name=".MalsseumHeaderContentActivity"/>
    </application>

</manifest>